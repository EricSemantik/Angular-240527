import { BackGroundBlackDirective } from './back-ground-black.directive';

describe('BackGroundBlackDirective', () => {
  it('should create an instance', () => {
    const directive = new BackGroundBlackDirective();
    expect(directive).toBeTruthy();
  });
});
